name: Build Windows

on:
  push:
    branches:
      - main
    tags:
      - 'v*'
  pull_request:
    branches:
      - main
  workflow_dispatch:

jobs:
  build-windows:
    runs-on: windows-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Setup MSVC
        uses: ilammy/msvc-dev-cmd@v1
        with:
          arch: x64

      - name: Cache Whisper model
        id: cache-whisper
        uses: actions/cache@v4
        with:
          path: resources/models/whisper
          key: whisper-model-ggml-small-en-q5_1

      - name: Download Whisper model
        if: steps.cache-whisper.outputs.cache-hit != 'true'
        run: |
          mkdir -p resources/models/whisper
          curl -L -o resources/models/whisper/ggml-small.en.q5_1.bin https://huggingface.co/ggerganov/whisper.cpp/resolve/main/ggml-small.en-q5_1.bin

      - name: Cache whisper.cpp
        id: cache-whisper-cpp
        uses: actions/cache@v4
        with:
          path: native/phantom-audio/whisper.cpp
          key: whisper-cpp-${{ hashFiles('native/phantom-audio/CMakeLists.txt') }}

      - name: Clone whisper.cpp
        if: steps.cache-whisper-cpp.outputs.cache-hit != 'true'
        run: |
          cd native/phantom-audio
          git clone --depth 1 https://github.com/ggerganov/whisper.cpp.git

      - name: Cache phantom-audio build
        id: cache-phantom-audio
        uses: actions/cache@v4
        with:
          path: native/phantom-audio/build
          key: phantom-audio-${{ hashFiles('native/phantom-audio/src/**', 'native/phantom-audio/CMakeLists.txt') }}

      - name: Build phantom-audio
        if: steps.cache-phantom-audio.outputs.cache-hit != 'true'
        run: |
          cd native/phantom-audio
          mkdir -p build
          cd build
          cmake .. -G "Visual Studio 17 2022" -A x64
          cmake --build . --config Release
          mkdir -p bin
          cp Release/phantom-audio.exe bin/

      - name: Install dependencies
        run: npm install

      - name: Build Windows executable (dev)
        if: ${{ !startsWith(github.ref, 'refs/tags/v') }}
        run: npx cross-env NODE_ENV=production rimraf dist dist-electron && npx vite build && npx tsc -p tsconfig.electron.json && npx electron-builder --win --publish=never
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and Publish Windows executable (release)
        if: ${{ startsWith(github.ref, 'refs/tags/v') }}
        run: npx cross-env NODE_ENV=production rimraf dist dist-electron && npx vite build && npx tsc -p tsconfig.electron.json && npx electron-builder --win --publish=always
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Upload Windows artifacts
        uses: actions/upload-artifact@v4
        with:
          name: windows-build
          path: |
            release/*.exe
            release/latest.yml

      - name: Create Release
        if: ${{ startsWith(github.ref, 'refs/tags/v') }}
        uses: softprops/action-gh-release@v1
        with:
          files: |
            release/*.exe
            release/latest.yml
          draft: false
          generate_release_notes: true
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
